import { Request, Response, NextFunction } from "express";

export const createVandor = async (
  req: Request,
  res: Response,
  next: NextFunction
) => {};

export const getAllVandor = async (
  req: Request,
  res: Response,
  next: NextFunction
) => {};

export const getVandorById = async (
  req: Request,
  res: Response,
  next: NextFunction
) => {};
